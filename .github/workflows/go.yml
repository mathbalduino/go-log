name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-go@v2
      with:
        go-version: 1.17

    - name: gofmt
      uses: Jerome1337/gofmt-action@v1.0.4
      with:
        gofmt-path: './'
        gofmt-flags: '-l -d -s -e'

    - name: get latest tag
      id: latest_tag
      uses: "WyriHaximus/github-action-get-previous-tag@v1"

    - name: check LibraryModuleVersion variable
      run: .github/checkVersionVar.sh ${{ steps.latest_tag.outputs.tag }}

    - name: go vet
      run: go vet ./...

    - name: go test
      run: go test -v -coverprofile=coverage.txt -covermode=atomic ./...

    - uses: codecov/codecov-action@v2
      with:
        files: ./coverage.txt
        fail_ci_if_error: true
        verbose: true
